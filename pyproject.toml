[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "elocal_analysis"
version = "0.1.0"
description = "Project scaffolded from cookiecutter-data-science"
readme = "README.md"
requires-python = ">=3.8"
